<resources>
    <!-- Neutral launch-window theme for the Compose MainActivity.
        All in-app theming is done via the Compose HomeFitTheme,
        so this theme only affects the starting window. -->
    <style name="Theme.HomeFit" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
